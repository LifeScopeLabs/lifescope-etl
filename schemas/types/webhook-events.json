{
  "id": "#/types/webhook-events",
  "title": "Webhook events",
  "description": "Schema for valid BitScoop webhook event types",
  "enum": [
    "connection_created",
    "connection_reauthorized",
    "connection_deleted"
  ]
}
